AOS.init({
                    duration: 1000,  // animation duration in ms
                    once: true,       // animate only once when scrolling
                    startEvent:'load' //Wait until Paige is fully loaded
                });